# 저전력 설정
CONFIG_ZMK_SLEEP=y
CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=900000  # 15분 후 절전 (밀리초)

# BLE 최적화
CONFIG_BT_GATT_ENFORCE_SUBSCRIPTION=n
CONFIG_ZMK_BLE_EXPERIMENTAL_CONN=y

# 배터리 절약
CONFIG_ZMK_IDLE_TIMEOUT=30000  # 30초 후 대기 모드
